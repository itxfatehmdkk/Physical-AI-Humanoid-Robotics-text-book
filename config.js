// Environment variables for Docusaurus build
const API_BASE_URL = process.env.API_BASE_URL || 'http://localhost:8000';

module.exports = {
  API_BASE_URL,
  // Other config values can be added here as needed
};